---
# install_pip/meta/main.yml

galaxy_info:
  role_name: install_pip
  author: "geerlingguy, straysheep-dev"
  description: Install pip via the system's package manager.
  license: license (MIT)
  min_ansible_version: "2.10"
  platforms:
    - name: Debian
      versions:
        - all
    - name: Ubuntu
      versions:
        - all
    - name: Kali
      versions:
        - all
    - name: Fedora
      versions:
        - all
    - name: Rocky
      versions:
        - all
  galaxy_tags:
    - system
    - server
    - packaging
    - python
    - pip
    - tools

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
